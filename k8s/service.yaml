apiVersion: v1
kind: Service
metadata:
  name: nerdprint
  namespace: nerdprint

spec:
  selector:
    app: nerdprint
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
